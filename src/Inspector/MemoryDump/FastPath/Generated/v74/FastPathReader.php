<?php

// AUTO-GENERATED by tools/generate-offset-readers.php — do not edit

declare(strict_types=1);

namespace Reli\Inspector\MemoryDump\FastPath\Generated\v74;

use Reli\Inspector\MemoryDump\FastPath\FastPathReader as FastPathReaderInterface;
use Reli\Inspector\MemoryDump\FastPath\RegionByteProvider;

/** @psalm-suppress PossiblyInvalidArrayAccess */
final class FastPathReader implements FastPathReaderInterface
{
    public function __construct(
        private RegionByteProvider $regions,
    ) {
    }

    #[\Override]
    public function zvalType(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 16);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 8]);
    }

    #[\Override]
    public function zvalValuePtr(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 16);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 0)[1];
    }

    #[\Override]
    public function zvalValueLval(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 16);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 0)[1];
    }

    #[\Override]
    public function zvalTypeInfo(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 16);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 8]) | (ord($region->bytes[$off + 8 + 1]) << 8) | (ord($region->bytes[$off + 8 + 2]) << 16) | (ord($region->bytes[$off + 8 + 3]) << 24);
    }

    #[\Override]
    public function bucketValType(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 32);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 8]);
    }

    #[\Override]
    public function bucketValValuePtr(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 32);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 0)[1];
    }

    #[\Override]
    public function bucketKeyPtr(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 32);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 24)[1];
    }

    #[\Override]
    public function bucketH(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 32);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 16)[1];
    }

    #[\Override]
    public function arrayArData(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 16)[1];
    }

    #[\Override]
    public function arrayFlags(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 8]) | (ord($region->bytes[$off + 8 + 1]) << 8) | (ord($region->bytes[$off + 8 + 2]) << 16) | (ord($region->bytes[$off + 8 + 3]) << 24);
    }

    #[\Override]
    public function arrayNTableMask(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 12]) | (ord($region->bytes[$off + 12 + 1]) << 8) | (ord($region->bytes[$off + 12 + 2]) << 16) | (ord($region->bytes[$off + 12 + 3]) << 24);
    }

    #[\Override]
    public function arrayNNumUsed(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 24]) | (ord($region->bytes[$off + 24 + 1]) << 8) | (ord($region->bytes[$off + 24 + 2]) << 16) | (ord($region->bytes[$off + 24 + 3]) << 24);
    }

    #[\Override]
    public function arrayNNumOfElements(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 28]) | (ord($region->bytes[$off + 28 + 1]) << 8) | (ord($region->bytes[$off + 28 + 2]) << 16) | (ord($region->bytes[$off + 28 + 3]) << 24);
    }

    #[\Override]
    public function arrayNTableSize(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 32]) | (ord($region->bytes[$off + 32 + 1]) << 8) | (ord($region->bytes[$off + 32 + 2]) << 16) | (ord($region->bytes[$off + 32 + 3]) << 24);
    }

    #[\Override]
    public function arrayRefcount(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 0]) | (ord($region->bytes[$off + 0 + 1]) << 8) | (ord($region->bytes[$off + 0 + 2]) << 16) | (ord($region->bytes[$off + 0 + 3]) << 24);
    }

    #[\Override]
    public function arrayTypeInfo(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 4]) | (ord($region->bytes[$off + 4 + 1]) << 8) | (ord($region->bytes[$off + 4 + 2]) << 16) | (ord($region->bytes[$off + 4 + 3]) << 24);
    }

    #[\Override]
    public function stringLen(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 32);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 16)[1];
    }

    #[\Override]
    public function stringH(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 32);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 8)[1];
    }

    #[\Override]
    public function stringRefcount(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 32);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 0]) | (ord($region->bytes[$off + 0 + 1]) << 8) | (ord($region->bytes[$off + 0 + 2]) << 16) | (ord($region->bytes[$off + 0 + 3]) << 24);
    }

    #[\Override]
    public function stringTypeInfo(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 32);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 4]) | (ord($region->bytes[$off + 4 + 1]) << 8) | (ord($region->bytes[$off + 4 + 2]) << 16) | (ord($region->bytes[$off + 4 + 3]) << 24);
    }

    #[\Override]
    public function objectCe(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 16)[1];
    }

    #[\Override]
    public function objectHandle(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 8]) | (ord($region->bytes[$off + 8 + 1]) << 8) | (ord($region->bytes[$off + 8 + 2]) << 16) | (ord($region->bytes[$off + 8 + 3]) << 24);
    }

    #[\Override]
    public function objectHandlers(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 24)[1];
    }

    #[\Override]
    public function objectPropertiesTable(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 40)[1];
    }

    #[\Override]
    public function objectProperties(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return unpack('P', $region->bytes, $off + 32)[1];
    }

    #[\Override]
    public function objectRefcount(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 0]) | (ord($region->bytes[$off + 0 + 1]) << 8) | (ord($region->bytes[$off + 0 + 2]) << 16) | (ord($region->bytes[$off + 0 + 3]) << 24);
    }

    #[\Override]
    public function objectTypeInfo(int $address): ?int
    {
        $region = $this->regions->regionFor($address, 56);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return ord($region->bytes[$off + 4]) | (ord($region->bytes[$off + 4 + 1]) << 8) | (ord($region->bytes[$off + 4 + 2]) << 16) | (ord($region->bytes[$off + 4 + 3]) << 24);
    }

    #[\Override]
    public function stringVal(int $address, int $len): ?string
    {
        $region = $this->regions->regionFor($address, 24 + $len);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($address);
        return substr($region->bytes, $off + 24, $len);
    }

    /** @var array<int, array{class_name: string, default_properties_count: int}> */
    private array $ce_cache = [];

    #[\Override]
    public function resolveClassEntry(int $ce_addr): ?array
    {
        if (isset($this->ce_cache[$ce_addr])) {
            return $this->ce_cache[$ce_addr];
        }
        $region = $this->regions->regionFor($ce_addr, 64);
        if ($region === null) {
            return null;
        }
        $off = $region->offsetOf($ce_addr);
        $name_ptr = unpack('P', $region->bytes, $off + 8)[1];
        $dpc = unpack('l', $region->bytes, $off + 32)[1];
        if ($name_ptr === 0) {
            return null;
        }
        $sregion = $this->regions->regionFor($name_ptr, 32);
        if ($sregion === null) {
            return null;
        }
        $soff = $sregion->offsetOf($name_ptr);
        $len = unpack('P', $sregion->bytes, $soff + 16)[1];
        $name = substr($sregion->bytes, $soff + 24, $len);
        $result = ['class_name' => $name, 'default_properties_count' => $dpc];
        $this->ce_cache[$ce_addr] = $result;
        return $result;
    }

    #[\Override]
    public function zvalSize(): int
    {
        return 16;
    }

    #[\Override]
    public function bucketSize(): int
    {
        return 32;
    }

    #[\Override]
    public function arraySize(): int
    {
        return 56;
    }

    #[\Override]
    public function stringHeaderSize(): int
    {
        return 24;
    }

    #[\Override]
    public function stringValOffset(): int
    {
        return 24;
    }

    #[\Override]
    public function objectHeaderSize(): int
    {
        return 56;
    }

    #[\Override]
    public function packedElementSize(): int
    {
        return 32;
    }

    #[\Override]
    public function isArrayUninitialized(int $flags): bool
    {
        return (bool)($flags & (1 << 3));
    }
}
